version: '3'

services:
  roaddisrupt-producer:
    container_name: roaddisrupt-producer
    build: .
    environment:
      KAFKA_BROKER_URL: broker:9092
      TOPIC_NAME: road_disruption
      SLEEP_TIME: 60

networks:
  default:
    external:
      name: kafka-network